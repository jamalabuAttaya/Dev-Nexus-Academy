## Summary

Describe the problem and the change made.

## Verification

- [ ] `npm run check` passes.
- [ ] `php artisan test` passes in `laravel-api` when backend code changed.
- [ ] Arabic RTL and English LTR behavior were considered.
- [ ] New external content includes a clear original source.
- [ ] No secrets, local environment files, or generated dependencies are committed.
